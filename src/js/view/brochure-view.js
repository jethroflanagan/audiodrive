__DEFAULT_IMPORTS__
var TrackingEvents = require('event/tracking-events');

// TODO all code is currently in scripts - move it over
var BrochureView = Backbone.View.extend({
	
	initialize: function () {
	},
});

return BrochureView;